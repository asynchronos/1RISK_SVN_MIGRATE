CREATE TABLE [dbo].[cust_class] (
    [cif]       DECIMAL (9)     NOT NULL,
    [principal] DECIMAL (16, 2) NOT NULL,
    [adjclass]  CHAR (1)        NOT NULL,
    [tdr]       BIT             NOT NULL
);

