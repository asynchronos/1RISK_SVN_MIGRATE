CREATE TABLE [dbo].[SME_EXPOSURE_TYPE] (
    [EXPOSURE_TYPE_ID] INT            IDENTITY (1, 1) NOT NULL,
    [DESCRIPTION]      NVARCHAR (200) NULL,
    [PRIORITY]         INT            NULL
);

